// components/ActivityLogContent.js

export default function ActivityLogContent() {
    return (
      <div>
        <h2 className="text-xl font-bold">Activity Log</h2>
        <p>View the history of your actions on the platform.</p>
        {/* Add a list of activities like post creation, comment actions, etc. */}
      </div>
    );
  }
  